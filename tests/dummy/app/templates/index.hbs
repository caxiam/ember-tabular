<h5>Example #1</h5>
<h1>Table - Default</h1>
{{#ember-table-jsonapi class="table-default" columns=columns modelType="user" bindModel=users hasActions="true" as |section|}}
    {{#if section.isBody}}
        {{#each users as |row|}}
            <tr>
                <td>{{row.username}}</td>
                <td>{{row.emailAddress}}</td>
                <td>{{row.firstName}}</td>
                <td>{{row.lastName}}</td>
                <td>
                    {{#if row.updatedAt}}
                        {{moment-format row.updatedAt 'MM/DD/YYYY'}}
                    {{else}}
                        {{moment-format row.createdAt 'MM/DD/YYYY'}}
                    {{/if}}
                </td>
                <td>
                    {{#link-to "index" class="btn btn-default btn-xs" role="button"}}
                        Edit
                    {{/link-to}}
                </td>
            </tr>
        {{/each}}
    {{/if}}
{{/ember-table-jsonapi}}

<h5>Example #2</h5>
<h1>Table - Basic - Global Filter</h1>
{{#ember-table-jsonapi class="table-basic-global-filter" columns=columns2 modelType="user" bindModel=users2 filter=filter as |section|}}
    {{#if section.isHeader}}
        {{ember-table-jsonapi-global-filter filter=filter filterProperty="username" filterPlaceholder="Search by Username"}}
    {{else if section.isBody}}
        {{#each users2 as |row|}}
            <tr>
                <td>{{row.username}}</td>
                <td>{{row.emailAddress}}</td>
                <td>{{row.firstName}}</td>
                <td>{{row.lastName}}</td>
                <td>
                    {{#if row.updatedAt}}
                        {{moment-format row.updatedAt 'MM/DD/YYYY'}}
                    {{else}}
                        {{moment-format row.createdAt 'MM/DD/YYYY'}}
                    {{/if}}
                </td>
            </tr>
        {{/each}}
    {{/if}}
{{/ember-table-jsonapi}}

<h5>Example #3</h5>
<h1>Table - Basic - Global Filter / Date Filter</h1>
{{#ember-table-jsonapi class="table-basic-global-date-filter" columns=columns2 modelType="user" bindModel=users3 filter=filter2 as |section|}}
    {{#if section.isHeader}}
        <div class="row">
            <div class="col-sm-6">
                {{ember-table-jsonapi-global-filter filter=filter2 filterProperty="username" filterPlaceholder="Search by Username" label="Username"}}
            </div>
            <div class="col-sm-6">
                {{ember-table-jsonapi-date-filter filter=filter2 filterProperty="updated-at" label="Last Updated"}}
            </div>
        </div>
    {{else if section.isBody}}
        {{#each users3 as |row|}}
            <tr>
                <td>{{row.username}}</td>
                <td>{{row.emailAddress}}</td>
                <td>{{row.firstName}}</td>
                <td>{{row.lastName}}</td>
                <td>
                    {{#if row.updatedAt}}
                        {{moment-format row.updatedAt 'MM/DD/YYYY'}}
                    {{else}}
                        {{moment-format row.createdAt 'MM/DD/YYYY'}}
                    {{/if}}
                </td>
            </tr>
        {{/each}}
    {{/if}}
{{/ember-table-jsonapi}}

<h5>Example #4</h5>
<h1>Table - Basic - Route Binds Model to Table</h1>
{{#ember-table-jsonapi class="table-basic-route-model" columns=columns2 makeRequest=false bindModel=model page=page pageLimit=pageLimit as |section|}}
    {{#if section.isBody}}
        {{#each model as |row|}}
            <tr>
                <td>{{row.username}}</td>
                <td>{{row.emailAddress}}</td>
                <td>{{row.firstName}}</td>
                <td>{{row.lastName}}</td>
                <td>
                    {{#if row.updatedAt}}
                        {{moment-format row.updatedAt 'MM/DD/YYYY'}}
                    {{else}}
                        {{moment-format row.createdAt 'MM/DD/YYYY'}}
                    {{/if}}
                </td>
            </tr>
        {{/each}}
    {{/if}}
{{/ember-table-jsonapi}}
