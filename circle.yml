# circle.yml
machine:
  node:
    version: 5.1.0

dependencies:
  pre:
    - npm install -g npm@5.0.2
    - npm install -g ember-cli@2.4.2
    - npm install -g phantomjs@2.1.1
    - npm install
    - bower install
  override:
    - npm i
    - bower i
