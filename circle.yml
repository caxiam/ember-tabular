# circle.yml
machine:
  node:
    version: 6.11.0

dependencies:
  pre:
    - npm install -g npm@5.4.2
    - npm install -g bower
    - npm install -g ember-cli@2.15.1
    - npm install
    - bower install
  override:
    - npm i
    - bower i

test:
  override:
    - JOBS=1 ember test